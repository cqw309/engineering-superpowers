## What Changed
Summary of the change, one or two sentences.

## Why
Link to the requirement/design doc (`docs/design/<feature-name>.md`). The motivation, not a restatement of the diff.

## Implementation
Key technical decisions worth calling out to a reviewer who hasn't read the design doc.

## Testing
- Unit tests: <what was added/changed>
- Integration tests: <what was added/changed>
- Regression: <what was manually verified, if anything>

## Risk
Anything a reviewer should be extra careful about; rollback plan if this goes wrong in production.
